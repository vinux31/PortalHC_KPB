# E2E Worker-Success Audit — Normal + PrePost, Single Answer (non-Proton)

**Tanggal:** 2026-06-14 · **Sifat:** URGENT focus · **Goal:** worker bisa ujian + sukses end-to-end.  
**Scope:** assessment Normal + PrePost, soal Single Answer, NON-Proton. Lensa: kerjaan v25-v28 belum-audit.  
**Metode:** 6 agent trace happy-path (normal+prepost) + filter 71 finding lama + planner. 8 agent.  
**Sumber detail:** docs/assessment-audit/2026-06-14-code-audit-findings.md.

## Verdict: Worker bisa sukses hari ini? **CONDITIONAL**

Belum. Worker BISA lulus ujian single-answer normal/prepost di JALUR HAPPY PALING UMUM (1 paket, semua soal terisi, token huruf-besar, satu tab). Tapi ada 3 show-stopper nyata di luar happy-path: (1) SHF-01 [v27 BARU] â€” default shuffle ON, kalau ada >=2 paket dan salah satu paket masih kosong (state authoring lumrah), K=Min(jumlah soal)=0 â†’ SEMUA worker dapat 0 soal, auto-grade 0% Fail; ini regresi v27 dan paling berbahaya. (2) TOK-01 [LAMA, ada di prod] â€” admin meng-edit token Pre/Post dan mengetik huruf kecil â†’ tersimpan lowercase, input worker dipaksa uppercase â†’ 'Token tidak valid' permanen, worker terkunci dari Pre (dan Post yang ter-gate). (3) Same-day Pre/Post [LAMA] â€” query sibling StartExam (Title+Category+Schedule.Date, TANPA filter AssessmentType) menyatukan paket Pre+Post jadi satu pool â†’ set soal salah/tercampur + denominator grading tercemar. Plus WRONG-RESULT: SAVE-01 (index unik di-drop â†’ dua baris MC duplikat saat concurrent â†’ GradingService FirstOrDefault tanpa ORDER BY bisa nilai opsi basi), STAT-01/STAT-02 (sesi Abandoned/Cancelled bisa di-resurrect jadi Completed-lulus / blind-UPDATE menimpa hasil graded balik ke Abandoned â†’ verdict hilang dari Records), TMR-01 (timer Standard dead-code â†’ submit telat diterima), CERT-01 (cert lulus ValidUntil=null tampil Expired + tak terhitung badge/notif). Rekomendasi: 3 fase minimal â€” Fase 1 entry-integrity (BLOCKER), Fase 2 grading-correctness (WRONG-RESULT, 1 migration), Fase 3 cert-visibility.

**Filter:** 18 finding in-scope, 53 out (Proton/essay/multi-answer/admin). Blocker E2E: 2 BLOCKER + 9 WRONG-RESULT. 2 temuan BARU (audit per-area lewat).

## Show-stopper (urut dampak E2E)

| Impact | Ref | Tipe | v25-28? | Prod? | Masalah |
|---|---|---|---|---|---|
| BLOCKER | TOK-01 | prepost | lama | in-prod | Pre/Post EditAssessment stores AccessToken without ToUpper -> token-required prepost worker permanently locked out |
| WRONG-RESULT | SHF-01 | normal+prepost | lama | in-prod | ON-path multi-package: a single empty package zeroes everyone's exam (K = Min = 0 -> 0 questions -> auto-grade 0% Fail recorded) |
| WRONG-RESULT | STAT-01 | normal+prepost | lama | in-prod | Grading guard does not exclude Abandoned/Cancelled â€” abandoned attempt resurrected into scored/passing Completed (single-answer) |
| WRONG-RESULT | STAT-02 | normal+prepost | lama | in-prod | AbandonExam blind UPDATE can overwrite a freshly-graded Completed session back to Abandoned -> worker's passing verdict disappears from Results/Records |
| WRONG-RESULT | SAVE-01 | normal+prepost | lama | in-prod | SaveAnswer atomic-upsert dead catch relies on a dropped unique index; concurrent saves create duplicate single-answer rows -> GradingService FirstOrDefault may grade a stale option (WRONG-RESULT) |
| WRONG-RESULT | NEW-package-render-shuffle-1 | prepost | lama | in-prod | Same-day Pre/Post: sibling query (Title+Category+Schedule.Date, no AssessmentType filter) pools BOTH Pre and Post packages into one worker's exam |
| WRONG-RESULT | CERT-01 | normal+prepost | lama | in-prod | Passed single-answer cert with ValidUntil=null (normal-mode default) is classified Expired on CMP/CDP cert dashboards and dropped from expiry badge/notifications |
| DEGRADED | TMR-03 | prepost | lama | in-prod | One-shot AutoSubmitToken consumed before grading commits â€” a transient DB failure on auto-submit attempt #1 turns retries into a permanent rejection (PreTest/PostTest only) |
| DEGRADED | OPS-01 | normal+prepost | BARU | in-prod | StartExam GET starts the worker's exam (StartedAt/InProgress + shuffle assignment locked) during impersonation â€” write-on-GET guard incomplete |
| MINOR | TOK-02 | normal+prepost | lama | in-prod | Token gate (IsTokenRequired) enforced only in StartExam lobby, not on SaveAnswer/SubmitExam â€” owner can bypass token |
| MINOR | NEW-answer-save-resume-1 | normal+prepost | lama | in-prod | No e2e/integration test asserts a saved single-answer selection survives reload/resume (resume restore correctness for MC is untested) |
| MINOR | TMR-01 | normal | lama | in-prod | Server-side timer-expiry guard is dead code for NORMAL exams (AssessmentType="Standard" never matches the Online/PreTest/PostTest allowlist) |
| MINOR | TMR-02 | normal+prepost | lama | in-prod | Incomplete-submission gate still trusts client isAutoSubmit flag (early submit of unanswered exam) |

## Rencana URGENT: 3 Phase

### Phase 1 — Fase 1 â€” Entry/StartExam integrity (BLOCKER: worker harus dapat MULAI ujian dengan set soal & token yang benar)
**~5 commit** · refs: SHF-01, TOK-01, NEW-package-render-shuffle-1, OPS-01, TOK-03

Empat cacat yang mencegah worker bahkan mendapatkan ujian yang sah, semuanya bertemu di jalur masuk StartExam GET + verifikasi token. (a) SHF-01 [v27 BARU, regresi v25-v28]: ShuffleEngine.BuildCrossPackageAssignment (Helpers/ShuffleEngine.cs:108-110) menghitung K=packages.Min(p=>p.Questions.Count) di ON-path TANPA memfilter paket kosong lebih dulu (cabang OFF justru memfilter di ShuffleEngine.cs:53-57). Default ShuffleQuestions=ON, jadi bila ada >=2 paket sibling dan satu paket masih 0 soal (state authoring lumrah), K=0 â†’ tiap worker dapat ShuffledQuestionIds kosong yang DIKUNCI di UserPackageAssignment saat StartExam (CMPController.cs:1019-1042); maxScore=0 â†’ auto-grade 0% Fail untuk seluruh batch. Fix: filter paket kosong sebelum hitung K, mirror cabang OFF. (b) TOK-01 [LAMA, ada di prod]: cabang Pre/Post EditAssessment menyimpan AccessToken verbatim tanpa .ToUpper() (AssessmentAdminController.cs:1812, 1916, 1937) lalu return di :1957 sebelum mencapai logika uppercase :2012; VerifyToken membandingkan AccessToken != token.ToUpper() (CMPController.cs:876) â†’ token lowercase tak pernah cocok â†’ worker terkunci permanen dari Pre & Post. Fix: normalisasi token (.ToUpper bila IsTokenRequired) di tiga titik tulis itu. (c) NEW-package-render-shuffle-1 [LAMA]: query sibling StartExam (CMPController.cs:982-987) match Title+Category+Schedule.Date TANPA filter AssessmentType, padahal Pre & Post dibuat dengan model.Title yang sama (AssessmentAdminController.cs:1220 & 1256); same-day â†’ pool paket Pre+Post tercampur, K=Min lintas Pre+Post, denominator grading tercemar. Fix: tambahkan filter AssessmentType (atau LinkedGroupId) pada query sibling supaya Pre dan Post tidak saling memungut paket. (d) OPS-01 + TOK-03 [v25-v28 BARU, Phase 377]: guard write-on-GET impersonasi hanya membungkus transisi Upcoming->Open (CMPController.cs:905); blok justStarted (CMPController.cs:961-967 set StartedAt+InProgress + SignalR workerStarted) dan blok create UserPackageAssignment (1012-1056) TIDAK dijaga, jadi admin yang sekadar membuka ujian worker X saat impersonasi membakar waktu X + mengunci set shuffle X. Dibundel di fase ini karena menyentuh handler StartExam GET yang sama. Fix: bungkus semua tulisan mutasi di StartExam GET dengan if(!IsImpersonating()). Regression test: unit ShuffleEngine (>=2 paket, satu kosong â†’ kembalikan soal dari paket berisi, BUKAN empty); unit/integration token Pre/Post edit lowercase â†’ tersimpan uppercase â†’ VerifyToken cocok; integration same-day Pre/Post â†’ pool paket Pre saja untuk sesi Pre; integration impersonasi membuka Open exam â†’ StartedAt tetap null & tak ada assignment.

### Phase 2 — Fase 2 â€” Grading & lifecycle correctness (WRONG-RESULT: skor/lulus/Records harus benar & tahan-race)
**~6 commit · +MIGRATION (flag IT)** · refs: SAVE-01, STAT-01, STAT-02, TMR-01

Empat cacat yang membuat hasil worker single-answer salah atau hilang meski worker menyelesaikan ujian. (a) SAVE-01 [LAMA]: migration 20260407070949 men-drop unique index (AssessmentSessionId,PackageQuestionId) (perlu untuk MultipleAnswer banyak-baris), sehingga catch DbUpdateException di SaveAnswer (CMPController.cs:387-400) jadi dead code; dua SaveAnswer konkuren (multi-tab/offline-flush) untuk (sessionId,questionId) yang sama dua-duanya updatedCount==0 â†’ dua baris MC dengan optionId berbeda; SubmitExam hanya menyentuh g.First() (CMPController.cs:1622-1624) lalu GradingService otoritatif baca ulang DB via FirstOrDefault TANPA ORDER BY â†’ bisa menilai opsi NON-final â†’ Score/IsPassed/cert salah. Fix: kembalikan unique index TERFILTER khusus tipe single-answer (filtered unique index, mis. WHERE pada tipe MC) ATAU dedupe baris MC (keep terbaru) sebelum grading; MIGRATION diperlukan. (b) STAT-01 [LAMA]: guard ExecuteUpdate di GradeAndCompleteAsync hanya kecualikan 'Completed' (GradingService.cs:238-246, cabang essay :202-206) â€” TIDAK kecualikan Abandoned/Cancelled, dan SubmitExam hanya blok =="Completed" (CMPController.cs:1545); sesi yang sudah di-Abandon worker / di-Cancel HC bisa di-resurrect jadi Completed-lulus + cert. Fix: tambah Abandoned/Cancelled (+PendingGrading) ke guard + early lifecycle guard di SubmitExam. (c) STAT-02 [LAMA]: AbandonExam (CMPController.cs:1233-1244) blind UPDATE tanpa WHERE-status â†’ bisa menimpa sesi yang baru saja Completed/graded balik ke Abandoned; Results/Records gate pada Status (IsAssessmentSubmitted) â†’ verdict lulus worker lenyap dari layar meski Score/cert tersimpan. Fix: ubah ke ExecuteUpdate ber-guard Where(Status==InProgress||Open) + cabang rowsAffected==0. (d) TMR-01 [LAMA, regresi Phase 313]: EnsureCanSubmitExamAsync (CMPController.cs:4390-4395) hanya enforce timer untuk Online/PreTest/PostTest, tapi 'Online' tak pernah di-assign; ujian Standard (mayoritas normal) lolos â†’ submit jauh-telat diterima & di-grade, integritas timer hilang. Fix: balik logika agar hanya SKIP untuk Manual/null (atau tambahkan 'Standard' ke allowlist). Regression test: integration concurrent SaveAnswer (2 request) â†’ tepat 1 baris MC final â†’ grade benar; unit/integration submit pada sesi Abandoned/Cancelled â†’ ditolak/tak ter-resurrect; integration AbandonExam vs Completed â†’ tidak menimpa; unit EnsureCanSubmitExam Standard elapsed>allowed tanpa token â†’ ditolak.

### Phase 3 — Fase 3 â€” Hasil & sertifikat single-answer terlihat benar (WRONG-STATE pasca-lulus)
**~3 commit** · refs: CERT-01

CERT-01 [LAMA]: worker yang LULUS ujian normal/prepost(PostTest) single-answer dengan GenerateCertificate=true namun ValidUntil dibiarkan null (default normal-mode, opsional) tetap dapat NomorSertifikat+PDF+LULUS di Results, TAPI DeriveCertificateStatus mengklasifikasikannya 'Expired' di dashboard CMP/CDP/Renewal (CertificationManagementViewModel.cs:58-59), sementara badge GetCertAlertCountsAsync (HomeController.cs:215) dan notifikasi TriggerCertExpiredNotificationsAsync (HomeController.cs:124) memfilter ValidUntil.HasValue sehingga cert null TIDAK dihitung & tak memicu notif. Hasilnya state kredensial yang ditampilkan kontradiktif: dashboard bilang Expired, badge undercount, renewal terlewat. Fix: satukan definisi â€” perlakukan ValidUntil==null sebagai 'tanpa kedaluwarsa/Permanen-Aktif' di DeriveCertificateStatus, ATAU sertakan cert null di hitung+notif Expired; terapkan konsisten di tiga titik. Tanpa migration (logika/query saja). Regression test: unit DeriveCertificateStatus(null,null) sesuai keputusan; unit/integration badge+notif konsisten dengan status dashboard untuk cert lulus ValidUntil=null.

## Playwright E2E Test Plan (bukti worker sukses)

1. NORMAL single-answer happy path (1 paket): login HC via helper login(page,'hc') â†’ createAssessmentViaWizard(category non-Proton/'OJT', AssessmentType Standard, durationMinutes>0, passPercentage 60, generateCertificate true, 1 peserta) â†’ createDefaultPackage + addQuestionViaForm 3-5 soal Single Answer (MultipleChoice, satu opsi IsCorrect, sebagian dengan gambar via _QuestionImage) â†’ login worker â†’ /CMP/Assessment â†’ StartExam â†’ assert semua soal Single Answer ter-render (radio + gambar) dengan jumlah == jumlah soal (BUKTI SHF tak zero) â†’ pilih semua jawaban benar (SaveAnswer) â†’ submitExamTwoStep â†’ Results assert Score==100 & LULUS & ada NomorSertifikat & tombol unduh PDF â†’ assert via db.snapshot AssessmentSession.Status='Completed', IsPassed=1, Score=100, NomorSertifikat!=null.

2. NORMAL resume/persist (regresi NEW-answer-save-resume-1): mulai ujian, jawab sebagian soal Single Answer, reload halaman StartExam â†’ assert radio yang sudah dipilih tetap ter-check (DB SavedAnswers->radio pre-check) â†’ lanjut jawab sisanya â†’ submit â†’ Score benar; membuktikan saved option bertahan lintas reload.

3. NORMAL grading-benar untuk jawaban campur (beberapa benar beberapa salah): jawab sebagian salah â†’ submit â†’ assert Score == (jumlah benar*ScoreValue / maxScore*100) dan IsPassed sesuai PassPercentage; gunakan ScoreValue non-uniform untuk memastikan denominator tertimbang benar.

4. PREPOST same-day happy path (regresi NEW-package-render-shuffle-1): createAssessmentViaWizard AssessmentType PrePostTest, Pre & Post scheduleDate SAMA (today()), SamePackage atau CopyPackagesFromPre, paket Pre berisi soal Single Answer â†’ login worker â†’ StartExam sesi PRE â†’ assert jumlah & teks soal == HANYA paket Pre (tidak tercampur paket Post) â†’ jawab benar â†’ submit â†’ assert Pre Completed/LULUS â†’ kemudian StartExam sesi POST â†’ assert hanya paket Post â†’ submit â†’ assert Post graded benar; membuktikan pool sibling tidak mencampur Pre+Post.

5. PREPOST token-required (regresi TOK-01): create Pre/Post token-required, lalu EDIT assessment via /Admin dan KETIK token berisi huruf kecil (mis. 'abc23x') â†’ simpan â†’ login worker â†’ /CMP/Assessment klik 'Masukkan Token' â†’ ketik token persis ('abc23x', UI memaksa uppercase) â†’ assert VerifyToken SUKSES & redirect StartExam (BUKAN 'Token tidak valid') â†’ selesaikan Pre single-answer â†’ assert LULUS; juga assert Post (ter-gate Pre) bisa diakses.

6. SHF-01 multi-package empty (regresi BLOCKER, shuffle ON default): create assessment dengan 2 paket di mana paket ke-2 SENGAJA dibiarkan 0 soal, ShuffleQuestions ON â†’ login worker â†’ StartExam â†’ assert worker MENERIMA soal dari paket berisi (count>0, BUKAN 0 soal / bukan halaman kosong) â†’ jawab â†’ submit â†’ assert Score>0 & bukan auto-0% Fail; varian negatif: tanpa fix, test ini menangkap 0-soal.

7. OPS-01/TOK-03 impersonasi read-only (regresi v25-v28): admin login lalu impersonate worker X (mode user) â†’ buka StartExam untuk ujian X yang berstatus Open & StartedAt==null & non-token â†’ assert TIDAK ada mutasi DB: AssessmentSession.StartedAt tetap null, Status tetap Open, tidak ada UserPackageAssignment dibuat, tidak ada SignalR workerStarted; lalu hentikan impersonasi, login sebagai X sungguhan â†’ StartExam â†’ assert baru saat ini StartedAt ter-set (timer mulai dari nol untuk worker).

8. STAT-01 anti-resurrection (regresi WRONG-RESULT): worker mulai ujian lalu AbandonExam (Status=Abandoned) â†’ coba POST SubmitExam langsung ke sesi itu â†’ assert ditolak & sesi TIDAK berubah jadi Completed/lulus & tidak ada cert; ulangi untuk HC AkhiriSemuaUjian (Cancelled) â†’ submit telat â†’ assert tetap Cancelled.

9. STAT-02 abandon tak menimpa graded (regresi WRONG-RESULT): selesaikan & grade ujian sampai Completed/LULUS â†’ lalu panggil AbandonExam pada sesi yang sama â†’ assert AbandonExam ditolak (rowsAffected==0 path) & Status tetap Completed & hasil/cert tetap tampil di Results/Records (verdict tidak lenyap).

10. SAVE-01 concurrent single-answer (regresi WRONG-RESULT, integration): kirim dua SaveAnswer untuk (sessionId,questionId) yang sama nyaris bersamaan dengan optionId berbeda (opsi final benar) â†’ submit â†’ assert tepat SATU baris PackageUserResponse untuk soal itu dan grading memakai opsi FINAL â†’ Score benar (bukan opsi basi).

11. TMR-01 timer enforcement Standard (regresi WRONG-RESULT): ujian Standard durasi pendek, biarkan timer lewat (manipulasi StartedAt mundur via seed/db), coba submit manual tanpa auto-submit token â†’ assert ditolak (Tier-1/Tier-2) & ada audit SubmitExamBlocked; bandingkan happy-path (dalam waktu) tetap diterima.

12. CERT-01 visibilitas cert (regresi WRONG-STATE): worker LULUS normal/PostTest single-answer dengan generateCertificate true tapi ValidUntil dibiarkan kosong â†’ assert Results menampilkan LULUS+NomorSertifikat+PDF, DAN dashboard CMP/CDP/Renewal menampilkan status konsisten dengan keputusan (Aktif/Permanen, bukan Expired) â†’ assert badge Home & notifikasi konsisten (tidak undercount/kontradiktif).

## Catatan / Provenance

PROVENANCE (diverifikasi via git terhadap origin/main): SHF-01 = BARU v25-v28 (Helpers/ShuffleEngine.cs + kolom ShuffleQuestions/ShuffleOptions ABSEN di origin/main; ini regresi v27 â€” paling kritikal & paling mudah salah karena default ON). OPS-01/TOK-03 = BARU v25-v28 (guard IsImpersonating di StartExam tidak ada di origin/main; introduced Phase 377, tapi guard hanya menutup 1 dari 3 titik tulis). AssessmentScoreAggregator = BARU v28 (relevan PASS-01 essay, OUT OF SCOPE untuk single-answer). TOK-01, NEW-same-day-Pre/Post, SAVE-01 (drop unique index migration 20260407070949), STAT-01, STAT-02, TMR-01, CERT-01 = LAMA (ada di origin/main / sudah di prod) â€” bukan regresi v25-v28 tapi tetap show-stopper/wrong-result di scope. \n\nMIGRATION: hanya Fase 2 (SAVE-01) yang butuh migration â€” re-introduksi FILTERED unique index pada PackageUserResponses(AssessmentSessionId,PackageQuestionId) terbatas tipe single-answer (jangan unique penuh â€” akan memecah MultipleAnswer banyak-baris yang justru jadi alasan index di-drop). Wajib notify Team IT dengan commit hash + flag migration sesuai DEV_WORKFLOW (developer TIDAK promosi DB Dev/Prod). \n\nDROPPED dari fase urgent (DEGRADED/MINOR, disebut untuk kelengkapan, BUKAN blocker happy-path): TOK-02 (token gate tak di-enforce di SaveAnswer/SubmitExam â€” owner bisa bypass proctoring, integritas saja), TMR-02 (gate incomplete-submit masih percaya client isAutoSubmit â€” self-harm), TMR-03 (one-shot AutoSubmitToken habis sebelum grading commit â€” butuh DB hiccup, recoverable via re-grade admin), NEW-answer-save-resume-1 (tak ada blocker, hanya gap tes â€” sudah dimasukkan sebagai skenario e2e di Fase 1/umum), RES-02 (X/Y benar vs Score% tak konsisten â€” display), GRD-02 (empty-MA SetEquals â€” tak terjangkau via validasi create/edit, LOW), RST-01 & RST-04 (AddExtraTime tanpa [Authorize(Roles)] + tanpa cap total â€” security/integritas tapi tidak menghalangi worker menyelesaikan; RST-01 HIGH security sebaiknya ditambal cepat di luar jalur ini, 1-baris atribut). \n\nCATATAN OPS-01: di trace fresh ditandai DEGRADED (tidak memblok worker non-impersonasi), tapi karena fix-nya kecil dan berada di handler StartExam GET yang sama dengan SHF-01, dan ia MERUSAK timing/state worker in-scope saat impersonasi, ia dibundel ke Fase 1 sebagai satu unit shippable, bukan fase terpisah. \n\nHAPPY-PATH STATUS: jalur paling umum (NORMAL, 1 paket berisi penuh, token uppercase/auto-generate atau tanpa token, satu tab, dalam waktu) BEKERJA hari ini â€” itulah sebab jawaban CONDITIONAL, bukan NO. Yang membuat 'belum reliable': default shuffle ON + paket kosong (SHF-01), admin edit token lowercase (TOK-01), penjadwalan Pre/Post same-day (sibling pool), dan race/concurrency (SAVE-01/STAT). \n\nFile kunci: Helpers/ShuffleEngine.cs (108-110 ON-path, 53-57 OFF-path acuan fix); Controllers/CMPController.cs (StartExam 887-1078, SaveAnswer 348-417, SubmitExam 1523-1724, AbandonExam 1220-1248, VerifyToken 850-884, EnsureCanSubmitExamAsync 4382-4444, sibling query 982-1000); Controllers/AssessmentAdminController.cs (Pre/Post token tulis 1812/1916/1937, uppercase acuan 2012-2013, Pre/Post create Title 1220/1256); Services/GradingService.cs (guard 238-246 & 202-211, cert 269-301); Helpers/CertificationManagementViewModel.cs (58-59); Controllers/HomeController.cs (124, 215); migration referensi Migrations/20260407070949_RemoveUniqueIndexOnPackageUserResponse.cs. Test harness siap: HcPortal.Tests/ (xUnit, mis. ShuffleReshuffleTests.cs, ImpersonationIdentityTests.cs, CertificateStatusTests.cs) + tests/e2e/ (exam-taking.spec.ts pakai helper wizard examTypes.ts + dbSnapshot, shuffle.spec.ts, impersonation.spec.ts).

## Finding OUT-OF-SCOPE (di-skip untuk goal ini)

- **BYP-01** — proton â€” concurrent instant bypass can create two active assignments; Proton year-bypass workflow, out of scope.
- **BYP-02** — proton â€” ExecuteInstantBypassAsync public, skips double-pending guard; Proton bypass, out of scope.
- **BYP-03** — proton â€” BypassSave writes a success-implying audit log on rejection; Proton bypass audit, out of scope.
- **CAT-02** — admin authoring â€” EditCategory lacks cycle/self-parent/depth guard; category CRUD governance, not worker take path.
- **CAT-03** — admin authoring â€” duplicate-name check global vs (ParentId,Name) index; category CRUD, not worker take path.
- **CAT-04** — admin authoring â€” untrimmed duplicate check â†’ 500 on AddCategory; category CRUD robustness.
- **CAT-05** — admin authoring â€” category rename doesn't cascade to denormalized strings; category CRUD/reporting governance.
- **CAT-06** — admin authoring â€” DeleteCategory leaves orphaned category strings; category CRUD governance.
- **CERT-02** — admin-not-on-worker-path â€” Passâ†’Failâ†’Pass admin regrade leaves ValidUntil null; triggered by admin answer-editing/regrade, not by the worker taking the exam.
- **CERT-03** — admin-not-on-worker-path â€” NomorSertifikat collision with manual TrainingRecord numbers; cross-table cert numbering with manual-entry namespace, governance.
- **CERT-04** — admin-not-on-worker-path â€” cascade cert-file delete lacks webroot containment (not exploitable); cascade-delete defense-in-depth.
- **CERT-05** — admin-not-on-worker-path â€” concurrent double-renewal of a source cert; renewal-chain admin workflow, not worker take/pass path.
- **EDT-01** — essay â€” bug only fires on essay-containing sessions (regrade zeroes manually-graded essay points); a pure single-answer exam has no essay to drop.
- **EDT-02** — essay/admin-edit â€” ValidUntil loss requires the Passâ†’Failâ†’Pass regrade cycle driven by the essay-aware edit path; admin answer-edit regrade, not the worker take path.
- **EDT-03** — admin authoring/edit â€” MC multi-select not enforced server-side only via crafted admin edit POST; admin edit-answers path, antiforgery+role gated, not worker take path.
- **ESS-01** — essay â€” blank essay scored 0 with no recourse; essay-only.
- **ESS-03** — essay â€” stale NomorSertifikat in the essay-finalize AJAX response; essay-grading lifecycle only.
- **ESS-04** — essay â€” SubmitEssayScore missing Essay-type guard; essay grading endpoint, admin-only, never read for single-answer scoring.
- **GAIN-01** — essay â€” per-ElemenTeknis breakdown understated only for essay-containing assessments after essay grading; no essay in a single-answer exam.
- **GAIN-02** — admin-not-on-worker-path â€” normalized gain forced to 100 masks regressions; Pre/Post gain analytics dashboards/exports, reporting governance not worker take/pass.
- **GAIN-03** — admin-not-on-worker-path â€” gain ToDictionary throws on duplicate PostTest per user; gain report/export robustness, not worker take path.
- **GRD-01** — essay â€” same essay-regrade root as EDT-01; corruption requires Essay questions in the session, not present in a single-answer exam.
- **GRD-03** — multiple-answer/authoring â€” import accepts single-correct MA vs create/edit â‰¥2; MA definition governance, admin authoring not on worker take path.
- **MAN-01** — admin-not-on-worker-path â€” BulkBackfill ToDictionary on non-unique NIP throws 500; admin bulk score entry, not worker take path.
- **MAN-02** — admin-not-on-worker-path â€” manual-entry IsPassed free admin input defaulting to Lulus; admin manual/bulk entry bypassing the worker exam, data governance.
- **MAN-03** — admin-not-on-worker-path â€” manual-entry duplicate guard TOCTOU with no DB constraint; admin manual entry, not worker take path.
- **MAN-04** — admin-not-on-worker-path â€” BulkBackfill coerces unparseable Score to 0; admin bulk entry data quality.
- **MAN-05** — admin-not-on-worker-path â€” cross-path manual dedup misses on date vs datetime; admin manual/bulk entry dedup.
- **MAN-06** — admin-not-on-worker-path â€” BulkBackfill accepts arbitrary LinkedGroupId; admin bulk entry validation.
- **OPS-02** — admin-not-on-worker-path â€” group-complete notification dedup substring suppression; HC notification delivery, not the worker take/pass path.
- **OPS-03** — essay â€” SaveTextAnswer lacks the timer-expired guard; essay answer save path only.
- **OPS-04** — admin-not-on-worker-path â€” LogPageNav writes ExamActivityLog with no ownership check; HC monitoring/forensics log pollution, hardcoded page_nav, no grade/answer impact on worker take path.
- **OPS-05** — proton/prepost-notification â€” NotifyIfGroupCompleted groups Pre/Post together on same-day; HC group-completed notification timing, not the worker take/pass path.
- **PASS-01** — essay â€” duplicate of GRD-01/EDT-01; essay-only trigger, single-answer finalize math is clean (PASS-02).
- **PASS-02** — NONE/informational â€” confirms the single-answer pass/fail math is clean with no drift; not a defect.
- **PEL-01** — proton â€” EditAssessment bulk-add creates Proton sessions without eligibility gate / unset ProtonTrackId/TahunKe; Proton eligibility, out of scope.
- **PEL-02** — proton â€” Pre/Post path bypasses the Proton eligibility gate; Proton gating, out of scope.
- **PEL-03** — proton â€” CreateAssessment gate doesn't verify active track assignment; Proton eligibility, out of scope.
- **PEL-04** — proton â€” cross-year gate fails open on empty TahunKe; Proton year-gate, out of scope.
- **REC-01** — admin-not-on-worker-path â€” mirror training deleted flat causing FK rollback; cascade-delete records admin governance.
- **REC-02** — admin-not-on-worker-path â€” mirror heuristic can hard-delete a legitimate record; cascade-delete records admin governance.
- **REC-03** — admin-not-on-worker-path â€” group-complete notification dedup substring collision; HC notification, not worker take/pass path (duplicate of OPS-02).
- **REC-04** — admin-not-on-worker-path â€” Pre/Post renewal child hard-deleted in cascade orphaning gain partner; admin cascade-delete + gain pairing, not worker take path.
- **REC-05** — admin-not-on-worker-path â€” AddManualAssessment writes cert files before SaveChanges (orphan files); admin manual entry robustness.
- **RES-01** — essay â€” graded essays always show 'Salah' in review; essay-only display defect.
- **RES-03** — multiple-answer â€” zero-correct MA scored correct-in-store but 'Salah' in review; MA-only degenerate case, not single-answer.
- **RST-02** — essay/admin-not-on-worker-path â€” Reset button shown for essay-pending sessions but ResetAssessment rejects that status; HC reset control for essay/upcoming sessions, not worker take path.
- **SAVE-03** — essay â€” duplicate essay rows from concurrent SaveTextAnswer block FinalizeEssayGrading; essay save/finalize lifecycle only.
- **SAVE-05** — admin-not-on-worker-path â€” UpdateSessionProgress clamp omits ExtraTime, under-reporting elapsed in HC export; reporting drift, worker timer/grading unaffected.
- **SHF-02** — admin-not-on-worker-path â€” ReshufflePackage on an Abandoned session leaves orphans/drops SavedQuestionCount; HC reshuffle action (worker take path protected by StartExam Abandoned block), data-hygiene for analytics.
- **SHF-03** — admin-not-on-worker-path â€” inconsistent Abandoned handling between single vs bulk reshuffle; HC reshuffle endpoints, not worker take path.
- **T3-01** — proton â€” reversing a Tahun 3 interview leaves a stale completion marker; Proton Tahun3 interview, out of scope.
- **TMR-04** — admin-not-on-worker-path â€” duplicate of SAVE-05: Clamp 3 omits ExtraTime, under-reporting Durasi Aktual in admin Excel export; reporting drift, not worker take/grade integrity.

---

## VERIFIKASI PLAN + PARALELISME (2026-06-14, pre-milestone)

Coverage: 18/18 in-scope tertangani (10 di phase + 8 keputusan: RST-01/04 quick-patch, TOK-02/TMR-02/TMR-03 fold ke P2, resume=e2e test, RES-02/GRD-02 backlog).

### Verdict paralel
Verdict: P1, P2, P3 CANNOT be run as fully-parallel GSD phases as proposed, because all three edit Controllers/CMPController.cs (soft same-file overlap = merge-conflict hazard for parallel branches), and P2 additionally needs CMPController methods (SaveAnswer/SubmitExam) that the TOK-02 ride-along would also want. The single biggest blocker to parallelism is CMPController.cs, not the phase boundaries themselves. Recommended execution: SEQUENTIAL on the CMPController axis, with one genuinely-parallel carve-out. Wave 1 can run TWO things in parallel because they share NO files: (a) SHF-01 in the standalone Helpers/ShuffleEngine.cs (zero file overlap with anything), and (b) the RST-01/RST-04 quick-patch in AssessmentAdminController.cs:AddExtraTime (disjoint from everything else). Then the CMPController-touching work runs sequentially: P1-StartExam (StartExam GET + EditAssessment token) -> P2-grading/lifecycle (CMPController SaveAnswer/SubmitExam/AbandonExam/EnsureCanSubmit + GradingService + migration) -> P3-cert (CMPController cert region + 5 other controllers + ViewModel). P3 only soft-overlaps CMPController in a distinct region, so P3 COULD overlap P2 in time IF executed on a rebased branch off P2, but the cleanest GSD-safe answer is to keep P2->P3 sequential to avoid CMPController.cs rebase churn. Intra-phase, P2 is the densest: SAVE-01 and STAT-01 both edit GradeAndCompleteAsync (same method) and SubmitExam (same method) â€” these MUST be coordinated as one coherent change, not split across parallel sub-agents. Migration (SAVE-01) must be the LATEST migration in the tree; since P1 has no migration there is no migration ordering conflict, but only ONE IT migration-flag notification is needed (after P2). Bottom line: maximize safe parallelism by extracting SHF-01 and RST-01/04 into Wave 1 (truly parallel), then serialize the three CMPController-heavy phases.

### Struktur final: 5 phase / 4 wave

| Wave | Phase | Depends | Migr | Refs |
|---|---|---|---|---|
| wave-1 | P0a â€” Shuffle empty-package zero-fix | none | - | SHF-01 |
| wave-1 | P0b â€” Authz quick-patch AddExtraTime | none | - | RST-01, RST-04 |
| wave-2 | P1 â€” Entry/StartExam integrity | P0a | - | TOK-01, NEW-package-render-shuffle-1, OPS-01, TOK-03 |
| wave-3 | P2 â€” Grading & lifecycle correctness +MIGRATION | P1 | YES | SAVE-01, STAT-01, STAT-02, TMR-01, TMR-02, TMR-03, TOK-02 |
| wave-4 | P3 â€” Cert visibility | P2 | - | CERT-01 |

Wave 1 = PARALEL (2 phase, zero file-overlap): P0a SHF-01 di ShuffleEngine.cs bareng P0b RST-01+RST-04 di AddExtraTime. Sisanya SEQUENTIAL karena semua sentuh CMPController.cs.

### Risiko / miss saat plan

- SHF-01 ordering vs P1: P1 StartExam (line 1019) CONSUMES ShuffleEngine.BuildQuestionAssignment. If SHF-01 (ShuffleEngine fix) ships AFTER P1 or in a parallel branch that isn't merged first, P1's StartExam regression test (>=2 packages, one empty -> non-empty result) will FAIL against the unfixed engine.
  -> Land SHF-01 (Wave 1) BEFORE P1 (Wave 2), or merge SHF-01 into P1's branch base. The supplied map already notes 'no CMPController edit required for SHF-01' â€” keep it standalone and merge first.
- TOK-02 placement (same-method cross-phase conflict): TOK-02 is conceptually token-gate hardening (belongs with P1 TOK-01/TOK-03) but its fix edits SaveAnswer:363-367 and SubmitExam:1539 â€” the EXACT methods P2 edits (SAVE-01, STAT-01, TMR-*). Putting TOK-02 in P1 creates a same-method conflict with P2.
  -> Assign TOK-02's code change to P2 (Wave 3), not P1. P1 stays pure StartExam/Edit-token. Document the rationale so the StartedAt-gate is implemented once, coherently, alongside the SubmitExam lifecycle guard (STAT-01) which is in the same handler.
- P2 intra-phase same-method density (GradeAndCompleteAsync & SubmitExam): SAVE-01 (reads/ORDER BY) and STAT-01 (status guard) both mutate GradeAndCompleteAsync; SAVE-01 (1622-1624 GroupBy.First) and STAT-01 (1545 guard) and TMR-* (1553-1595) all mutate SubmitExam. If P2 is split across parallel sub-agents they will collide.
  -> Execute P2 as a single coherent phase (no intra-phase parallel sub-agents on CMPController/GradingService). Sequence commits: migration+index (SAVE-01 schema) -> SAVE-01 reads -> STAT-01 guards -> STAT-02 -> TMR-01/02/03 -> TOK-02.
- Filtered-unique-index migration feasibility (SAVE-01): A filtered unique index on PackageUserResponses(AssessmentSessionId,PackageQuestionId) restricted to single-answer rows requires a discriminator column ON the response table OR a join-filter on PackageQuestion.QuestionType. If no type column exists on PackageUserResponses, EF Core HasFilter cannot reference a joined table â€” may force raw-SQL filtered index or a denormalized type flag column (itself an extra schema change).
  -> Confirm during P2 planning whether PackageUserResponses has (or needs) a QuestionType/IsMultipleAnswer discriminator. If not, decide: (a) add a denormalized column (bigger migration) or (b) the dedupe-on-submit approach (ORDER BY SubmittedAt desc / last-write-wins, no migration). The report offers dedupe as the no-migration fallback â€” keep it as the contingency.
- Single IT migration notification + DEV_WORKFLOW: Only P2 has a migration. Per CLAUDE.md/DEV_WORKFLOW, developer must NOT promote DB Dev/Prod â€” must notify IT with commit hash + migration flag. Risk: forgetting the flag, or P3 accidentally scaffolding a snapshot-diff migration.
  -> Flag IT exactly once after P2 with the migration name + commit hash. Verify P1 and P3 produce migration=false (P3 ViewModel/controller/test edits must not trigger a model-snapshot diff). The new index migration MUST be the latest in the tree.
- CERT-01 null-handling decision is cross-surface (renewal worklist drop-out): If DeriveCertificateStatus stops returning Expired for ValidUntil==null certs, AdminBaseController post-filter (198-203, keeps only Expired||AkanExpired) will DROP those null certs from the renewal worklist, and Renewal/CDP count tallies auto-shift. This could silently remove legitimately-renewable certs from a surface â€” or correctly remove permanent certs that should never expire. Intent must be explicit.
  -> Lock the null-semantics decision in P3 planning (treat null = Permanent/no-expiry AND exclude from renewal/notif, vs include null in expiry). Update ALL consumers consistently: helper, HomeController badge+notif filters (123-124/215), AdminBase post-filter, Renewal/CDP tallies, and the CertificateStatusTests (the existing test DeriveCertificateStatus_NullValidUntil_NonPermanent_ReturnsExpired WILL break and must be rewritten to the new decision).
- Test that needs BOTH P1 and P2 (PrePost same-day E2E): E2E scenario #4 (PrePost same-day, Pre pool not mixed with Post) validates NEW-same-day-PrePost (P1) but also asserts Pre Completed/LULUS + Post graded correctly, which depends on grading correctness (P2 STAT/SAVE) and timer (TMR-01). Running it after P1 only may pass shuffle-pool but flake on grading edges.
  -> Treat the full PrePost E2E as a Wave-3 (post-P2) acceptance test, not a P1 gate. P1's own regression should assert only the sibling-pool narrowing (Pre query returns Pre packages only); full pass/grade assertions belong after P2.
- OPS-01/TOK-03 are effectively one change: TOK-03 has no distinct code site beyond OPS-01 (both = wrap StartExam GET write-on-GET in if(!IsImpersonating()) at 961-978 and 1012-1056). Risk of double-counting as two work items or fragmenting the guard.
  -> Implement as ONE atomic edit to StartExam GET in P1. ImpersonationService.IsImpersonating() already exists (line 35) â€” no service edit. Mirror the existing Phase-377 precedent at line 905 (Upcoming->Open already guarded).

### File overlap matrix

- [soft] Controllers/CMPController.cs <- P1, P2, P3
- [none] Services/GradingService.cs <- P2
- [none] Controllers/AssessmentAdminController.cs <- P1
- [none] Helpers/ShuffleEngine.cs <- P1
- [none] Data/ApplicationDbContext.cs <- P2
- [none] Models/CertificationManagementViewModel.cs <- P3
- [none] Controllers/HomeController.cs / CDPController.cs / AdminBaseController.cs / RenewalController.cs <- P3
