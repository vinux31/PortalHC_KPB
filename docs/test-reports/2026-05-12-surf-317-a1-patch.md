# Phase 318 Plan 01 — SURF-317-A1 Patch Report

**Date:** 2026-05-12
**Phase:** 318
**Plan:** 01
**Scope:** SURF-317-A1 single-file selector patch — `tests/e2e/exam-taking.spec.ts:40`
**Baseline:** `docs/test-reports/2026-05-11-flow-a-j-regression.md`

## Patch Detail

**File:** `tests/e2e/exam-taking.spec.ts` line 40 (single-line edit; lines 41-57 untouched).

BEFORE:
```typescript
await page.locator('.user-check-item', { hasText: 'rino.prasetyo' }).locator('input').click({ force: true });
```

AFTER:
```typescript
await page.locator('.user-check-item[data-email="rino.prasetyo@pertamina.com"] input.user-checkbox').check({ force: true });
```

**LOC delta:** 1 line modified.

**Rationale:**
- `[data-email="..."]` attribute selector matches Phase 304+ form-check wrapper (DOM verified — Playwright locator resolution log confirms input id `user_4a624dbc-3241-4207-92d7-d1d5784c7137` with class `form-check-input user-checkbox`).
- `input.user-checkbox` matches actual input class (Phase 304+ Bootstrap 5 form-check markup).
- `.check({ force: true })` semantic action — bypasses CSS visibility check (form-check label-wrap pattern hides raw input but element remains functionally interactive).

## FLOW A1 Status

- Pre-patch: CASCADE-ABORT at line 40 selector (force-click `.user-check-item input` legacy).
- Post-patch: **STILL FAIL — different root cause** (`Element is not visible` on `.check({ force: true })`).

**New diagnostic finding:** `/Admin/CreateAssessment` page sekarang serve **4-step wizard** (Phase 304+ refactor), bukan flat-page legacy markup yang FLOW A1 assume. Test navigates ke `/Admin/CreateAssessment` → lands di Step 1 "Kategori". Step 2 "Peserta" disabled (`<button [disabled]>2. Peserta</button>`) sampai Step 1 isi Category/Title + Next.

Selector patch line 40 **resolves to correct DOM element** (`<input name="UserIds" class="form-check-input user-checkbox" id="user_4a624dbc...">`) — element ada di DOM Step 2 panel tapi panel hidden via wizard step-toggle.

Implikasi: A1 PASS unreachable tanpa full FLOW A1 rewrite (lines 34-57) menggunakan helper `createDefaultAssessmentViaWizard` pattern Phase 317. Scope guardrail D-318-01 explicit prohibit extension — defer **full FLOW A wholesale refresh ke Phase 320**.

## Phase 317 Regression Gate

- Command: `cd tests && npx playwright test exam-types.spec.ts --reporter=list`
- Result: **28/28 PASS** (2.0m runtime — within baseline)
- Setup + W0.1, W0.2 + FLOW K (5) + FLOW L (6) + FLOW M (5) + FLOW N (4) + FLOW O (5) = 28 sub-tests HIJAU
- Teardown: matrix snapshot RESTORE OK, 0 matrix rows post-restore, SEED_JOURNAL cleaned
- **Phase 317 baseline preserved — patch tidak regress.**

## Cascade Delta (exam-taking.spec.ts)

| Metric | Baseline (2026-05-11) | Post-patch (2026-05-12) |
|--------|----------------------|-------------------------|
| Total tests | 76 (1 setup + 75 flows) | 76 (1 setup + 75 flows) |
| Setup passed | 1 | 1 |
| FLOW tests passed | 0 | 0 |
| FLOW tests failed | 1 (A1 cascade-anchor) | 1 (A1 — wizard mismatch) |
| Cascade-skipped | 74 | 74 |

### A1 Failure Mode Comparison

| Aspect | Pre-patch | Post-patch |
|--------|-----------|------------|
| Selector resolution | `.locator('.user-check-item', { hasText: ... }).locator('input')` — multi-match `<a>` element pre-step-2 wizard markup | `.user-check-item[data-email="..."] input.user-checkbox` — resolves correctly to `<input id="user_4a624dbc...">` |
| Failure stage | Selector force-click on wrong element | `.check({ force: true })` visibility check fail (Step 2 panel hidden) |
| Root cause | Stale Bootstrap 4 selector pattern | Wizard 4-step architecture (lines 34-57 assume flat page) |

### Cascade-skipped tests (74)

Sama dengan baseline 2026-05-11 (file-level serial mode `test.describe` cascade abort). Tests A2-A8, B1-B8, C1-C7, D1-D7, E1-E4, F1-F6, G1-G3, H1-H8, I1-I5, J1-J8, plus Phase 313 1-7 = 74 sub-tests skipped.

### New SURF-317-{n} Anchors (Phase 320 backlog)

#### SURF-317-A1 — UPDATED disposition
- **Anchor type:** Test fixture wholesale refresh
- **Original disposition (Phase 317):** Single-line selector patch line 40 → A1 PASS
- **Revised disposition (Phase 318 Plan 01 diagnostic):** Single-line patch insufficient. Full FLOW A1 (lines 34-57) requires wizard helper migration.
- **Patch applied:** Line 40 selector + `.check({ force: true })` — **kept** for correctness even though A1 tidak PASS. Patch matches Phase 304+ markup baseline; obsolete force-click eliminated.
- **Target phase:** Phase 320 wholesale FLOW A-J refresh

#### Other SURF-317-{n} candidates (deferred, no new anchor required)
A2..J cascade preserved; no new failure modes surfaced (no test executed beyond A1).

## Out of Scope (Phase 320 territory)

Per CONTEXT.md D-318-01 + scope guardrail, patch terbatas line 40. Obsolete selectors lain (DETECTED via inspection, BUKAN executed):

- `/Admin/CreateAssessment` flat page → 4-step wizard `/Admin/CreateAssessment` (same URL, refactored UI Phase 304+) — assume Step 1 Kategori, Step 2 Peserta, Step 3 Settings, Step 4 Konfirmasi
- `#Title` flat → wizard step-bound `#Title` (still exists at Step 1 in same DOM ID? — verify Phase 320)
- `#Category` `<select>` legacy → wizard `#Category` (Step 1)
- `#ScheduleDate` single input → wizard `#schedDateInput` + `#schedTimeInput` dual-input (Step 3)
- `#submitBtn` single-page submit → wizard `#btnSubmit` Step 4 final + `#btnNext` step-progression
- `name="question_text"` + `name="options[]"` → `name="questionText"` + `name="optionA..D"` (Phase 298+)
- `input[name="correct_option_index"][value="N"]` → `#correct_A/B/C/D` (Phase 298+)
- `/Admin/ManageQuestions` route → `/Admin/ManagePackageQuestions?packageId={N}` (Phase 298+)
- Wizard auto-create package → manual `createDefaultPackage` required (Phase 317 W0.1 discovery)
- 9 other identical `.user-check-item input.click({ force: true })` instances di file (lines 320, 407, 552, 706, 837, 967, 1061, 1295, 1408) — kept obsolete per scope guard, refresh Phase 320

## TypeScript Compile Gate

- Command: `cd tests && npx tsc --noEmit`
- Result: **exit 0** (clean)

## Next Steps

- Phase 318 Plan 02 (Wave 1 parallel) — SURF-317-A production fix (`CMPController.cs:2190` `ToLookup` refactor + Razor view update + Phase 317 MA regression rerun gate)
- Phase 320 (proposed) — wholesale FLOW A-J refresh menggunakan helpers `examTypes.ts` Phase 317 (createDefaultAssessmentViaWizard, MA/Essay/Mixed wizard sequences)
